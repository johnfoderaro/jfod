#!/usr/bin/env node

'use strict';

const metalsmithBuild = require('./../lib/metalsmith');

metalsmithBuild((err) => {
  if (err) console.error(err);
  else console.log('Metalsmith build complete.');
});
