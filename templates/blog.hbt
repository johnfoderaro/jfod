{{> head }}
<body>
  <div class="container">
    {{> navigation }}
    <div class="page">
      <main>
        {{{contents}}}
        <div class="posts">
        {{#each collections }}
        {{#each this }}
          <h3><a href="/{{ this.path }}">{{ this.headline }}</a> on {{ this.date }}</h3>
        {{/each}}
        {{/each}}
        </div>
      </main>
    </div>
    {{> footer }}
  </div>
  {{> scripts }}
</body>
</html>
